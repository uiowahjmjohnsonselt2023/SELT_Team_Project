version: "3.8"

name: "selt-team-project"

services: 
  ruby:
    build: ./
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    working_dir: /app
    volumes: 
      - .:/app
    ports: 
      - "3000:3000"

